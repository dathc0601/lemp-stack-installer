# Nginx security headers snippet
# Included by every server block via: include <snippets-dir>/security-headers.conf;
#
# Will be populated during module migration with:
#   X-Frame-Options, X-Content-Type-Options, Referrer-Policy,
#   X-XSS-Protection, Permissions-Policy, HSTS, server_tokens off
